{
    "name": "Custom",
    "description": "Customized container",
    "long_description": "Make your own container, with your own commands.",
    "icon": "gears.png",
    "folder": "custom",
    "variables": ["name", "extra_params:", "docker_image:alpine", "cmd:/bin/sh"],
    "cmds": [
        "docker run -d --name ${name} ${extra_params} ${docker_image} ${cmd}"
    ]
}